MLOps model API deployed as "{{ include "mlmodel.fullname" . }}" in namespace "{{ include "mlmodel.namespace" . }}".

{{- if .Values.ingress.enabled }}
Ingress hosts:
{{- range .Values.ingress.hosts }}
  http://{{ .host }}
{{- end }}
{{- else }}
Get the application URL by running these commands:
  export POD_NAME=$(kubectl get pods --namespace {{ include "mlmodel.namespace" . }} -l "app.kubernetes.io/name={{ include "mlmodel.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ include "mlmodel.namespace" . }} port-forward svc/{{ include "mlmodel.fullname" . }} 8080:{{ .Values.service.port }}
  echo "Visit http://127.0.0.1:8080/docs"
{{- end }}

Try it:
  curl -s http://127.0.0.1:8080/health
  curl -s -X POST http://127.0.0.1:8080/predict \
    -H "Content-Type: application/json" \
    -d '{"features":[5.1,3.5,1.4,0.2]}'
